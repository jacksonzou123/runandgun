class weapon{
  friendlyBullet bullet;
  int ammo;
  
  weapon(friendlyBullet bullet, int ammo) {
    this.bullet = bullet;
    this.ammo = ammo;
  }//this is a work in progress for later if we ever make more weapons
}
